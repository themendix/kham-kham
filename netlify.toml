[build]
  command = "npm run build"
  publish = "dist"

# Routage React Router en mode history : toute route profonde (/cours/:id, /biblio/:categoryId…)
# doit être servie par index.html, sinon un accès direct renvoie un 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
